spring:
  h2:
    console:
      enabled: true
  sql.init.mode: always
  datasource:
    url: jdbc:h2:file:./db/filmorate
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

logging.level:
  ru.practicum.filmorate: DEBUG
